# Skill — Deployment

VPS/Docker/Coolify uyumlu yapı hazırlanır. `.env.example`, Dockerfile, docker-compose.yml, README ve build komutları oluşturulur. Dolibarr base URL/API key environment üzerinden yönetilir. Production’da API key loglanmaz.
